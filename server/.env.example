# Server port
PORT=5000

# MongoDB connection string (MongoDB Atlas or local)
MONGO_URI=mongodb+srv://sanujiweerasinghe_db_user:Sanuji2000@cluster0.xxxxx.mongodb.net/portfolio?retryWrites=true&w=majority

# Comma-separated list of allowed CORS origins (client URL)
CLIENT_URL=http://localhost:5173

# Optional — email notifications via Nodemailer when a contact message is received.
# If left blank, messages are still saved to MongoDB but no email is sent.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
NOTIFY_EMAIL=sanujiweerasinghe@gmail.com
